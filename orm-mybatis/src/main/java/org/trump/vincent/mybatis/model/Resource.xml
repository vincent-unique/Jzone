<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.trump.vincent.mybatis.model.ResourceMapper">
    <select id="findById"
            parameterType="java.lang.String"
            resultType="org.trump.vincent.mybatis.model.Resource"
            useCache="false">
        SELECT * FROM COM_BASICINFO WHERE ID = #{id}
    </select>
    
    <select id="loadUsers"
            parameterMap=""></select>
</mapper>