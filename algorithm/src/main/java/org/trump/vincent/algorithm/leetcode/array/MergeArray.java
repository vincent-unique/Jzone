package org.trump.vincent.algorithm.leetcode.array;

public class MergeArray {

    /**
     * Problem_1:
     * Desc: 两个数组A、B, A的长度是两数组实际长度之和，且两数组都是有序数组；
     *      求在不扩展多余空间的情况下，将两者合并为新的有序数组
     *
     * Solution：
     *
     * @param lon
     * @param thin
     * @return
     */
    public static int[] mergeSortedArray(int[] lon, int[] thin ){

        return lon;
    }
}
